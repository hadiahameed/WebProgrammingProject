{{!-- MULTIPLE BOOKS DISPLAYED, FOR EXAMPLE: AFTER THE USER PERFORMS A SEARCH --}}
{{#extend "loggedIn"}}
{{#content "content"}}
<div class="container mt-3">
    <div class="row">
        <button type="button" class="btn btn-primary"><a href="/books/new" class="text-white"><i class="fas fa-plus"></i>
                Add new book</a></button>
        {{#each books as |book|}}
        <div id="booksList">
            <img src={{this.image}} alt="/public/resources/no-img-icon.jpeg" class="image">
            <div>
                <a href="/books/{{this._id}}">
                    <h1 id="bookTitle">Title: {{this.title}} </h1>
                </a>
                <h2 id="author">Author: {{this.author}} </h2>
                <p id="rating">Rating: {{this.rating}} </p>
                {{#if book.review}}
                <p id="review">Latest Review:
                    {{#each book.review}}
                    {{#if @last}}{{this}}{{/if}}
                    {{/each}}
                </p>
                {{/if}}
                {{#if this.tags}}
                <p id="tags">Tags: {{#each book.tags}}<span class="oneTag">{{this}}</span> {{/each}} </p>
                {{else}}
                <p>No tags</p>
                {{/if}}
                <button id="{{book._id}}" class="btn btn-danger glyphicon glyphicon-trash deleteBook"><i class="far fa-trash-alt"></i>
                    Delete</button>
            </div>
        </div>
        {{/each}}
    </div>
    {{#if hasErrors}}
    <div class="alert alert-danger error-list" role="alert">
        {{error}}
    </div>
    {{/if}}
</div>

{{/content}}
{{#content "scripts" mode="append"}}
<script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.0/dist/jquery.validate.min.js"></script>
<script src="/public/scripts/books.js"></script>
{{/content}}
{{/extend}}